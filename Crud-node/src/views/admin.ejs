<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Formulario admin</title>
    <!-- Agrega los enlaces a los estilos de Bootstrap -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://bootswatch.com/4/flatly/bootstrap.min.css">
    <link href="/website/css/uicons-solid-rounded.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/4c6085e56b.js" crossorigin="anonymous"></script>
</head>
<body>

<div class="container mt-4">
    <!-- Formulario -->
    <form action="/addA" method="POST">
        <br>
        <input type="hidden"  class="form-control" id="id">
        <div class="form-group">
            <label for="nombres">Nombres:</label>
            <input type="text" class="form-control"  id="nombres" name="nombres" required>
        </div>
        <div class="form-group">
            <label for="apellidos">Apellidos:</label>
            <input type="text" class="form-control"  id="apellidos" name="apellidos" required>
        </div>
        <div class="form-group">
            <label for="username">Nombre de Usuario:</label>
            <input type="text" class="form-control" id="Nombre_usuario" name="Nombre_usuario" autocomplete="off" required>
        </div>
        <div class="form-group">
            <label for="password">Contraseña:</label>
            <input type="password" class="form-control" id="contraseña"  name="contraseña" autocomplete="off" required>
        </div>
        <div class="form-group">
            <label for="correo">Correo:</label>
            <input type="email" class="form-control" id="correo"  name="correo" required>
        </div>
        <button type="submit"  class="btn btn-success" id="btnCreate">Crear</button>
    </form>
    <p> </p>
    <!-- Tabla -->
    <table class="table mt-4">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombres</th>
                <th>Apellidos</th>
                <th>Nombre de Usuario</th>
                <th>Contraseña</th>
                <th>Dirección</th>
                <th>Correo</th>
                <th>Teléfono</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <!-- Aquí se agregarán las filas de la tabla dinámicamente -->
            <%  if (data){ %>
              <%  for(i=0; i<data.length; i++){%>
                <tr>
                    <td><%=data[i].id %></td>
                    <td><%=data[i].nombres %></td>
                    <td><%=data[i].apellidos %></td>
                    <td><%=data[i].Nombre_usuario %></td>
                    <td>*****</td>
                    <td><%=data[i].direccion %></td>
                    <td><%=data[i].correo %></td>
                    <td><%=data[i].telefono %></td>
                    <td><a  href="/deleteA/<%= data[i].id %>" class="fa-solid fa-trash-can" style="color: #c71919e3;" ></a> <a href="/updateA/<%= data[i].id %>" style="position: relative; left: 30px;  color: #1613d1b2;" class="fa-solid fa-pen-to-square"></a></td>
                </tr>
            <%}} %>
              
            
        </tbody>
    </table>
</div>

<!-- Agrega los scripts de Bootstrap y jQuery -->
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>

</body>
</html>
