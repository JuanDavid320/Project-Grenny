<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://bootswatch.com/4/flatly/bootstrap.min.css">
  <script src="https://kit.fontawesome.com/4c6085e56b.js" crossorigin="anonymous"></script>
  <title>Info</title>
  <style>
    body {
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      height: 100vh;
      margin: 0;
    }

    h1{
      text-align: center;
    }

    .container {
      text-align: left; /* Alineación izquierda */
      margin-top: 30px; /* Ajustar según sea necesario */
    }

    .dato {
      margin-bottom: 10px;
    }

    #myChart {
   
      max-width: 500px; /* Ajustar según sea necesario */
      margin-top: 200px; /* Ajustar según sea necesario */
    }
    #myChart2{
        max-width: 500px; /* Ajustar según sea necesario */
      margin-top: 200px; /* Ajustar según sea necesario */
    }
  </style>
</head>
<body>
  
  <h3  style="text-align: center;"><i class="fa-solid fa-arrow-trend-up"></i> - Graficas</h3>
  <canvas style="position: relative; left: 300px; top: -100px;" id="myChart"></canvas>
</div>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
  const ctx = document.getElementById('myChart');
  const dataFromBackend = <%- JSON.stringify(data2) %>;
  const dataValues = dataFromBackend.map(item => item.Data);
  const labels = dataFromBackend.map((item) => `${item.Date}`);
  
  new Chart(ctx, {
    type: 'bar',
    data: {
      labels: labels,
      datasets: [{
        label: 'Datos del consumo',
        data: dataValues ,
        borderWidth: 1
      }]
    },
    options: {
      scales: {
        y: {
          beginAtZero: true
        }
      }
    }
  });
</script>
<br>
<br>
<canvas style="position: relative; left: 300px; top: -100px;" id="myChart2"></canvas>
</div>
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<script>
  const ctx2 = document.getElementById('myChart2');
  const dataFromBackend2 = <%- JSON.stringify(data2) %>;
  const dataValues2 = dataFromBackend2.map(item => item.DataT);
  const labels2 = dataFromBackend.map((item) => `${item.Date}`);
  
  new Chart(ctx2, {
    type: 'bar',
    data: {
      labels: labels2,
      datasets: [{
        label: 'Datos del consumo',
        data: dataValues2 ,
        borderWidth: 1
      }]
    },
    options: {
      scales: {
        y: {
          beginAtZero: true
        }
      }
    }
  });
</script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
