const jsonObject = {"battery": 93, "daylight": "dark", "pir": "trigger"};

// Obtener las claves del objeto
const keysArray = Object.keys(jsonObject);

// Mostrar las claves
  console.log("Nombre de la clave:", keysArray[0]);

